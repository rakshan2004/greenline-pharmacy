# Greenline Pharmacy management system.
# This file marks the "greenline" folder as a Python package so its modules
# (config, database, theme, auth and the ui sub-package) can be imported with
# dotted paths such as "from greenline import database".
